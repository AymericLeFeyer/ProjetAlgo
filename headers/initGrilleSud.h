#include "../headers/structure.h"

grilleSudokuBrute initGrilleSud ( int choix); //choix va de 0 a 13 pour un choix de difficulté sur la grille

int intAlea(int n);

int verification (grilleSudokuBrute g);
